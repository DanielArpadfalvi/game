{
  "name": "rift-dodge",
  "version": "0.1.0",
  "private": true,
  "description": "LoL-inspired browser skillshot-dodging arena built with Phaser 3 + TypeScript",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "fetch-assets": "node scripts/fetch-assets.mjs"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
